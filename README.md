# File-Integrity-Checker
Scans a directory, records SHA-256 hashes of all files, then lets you check later if any file was modified, added, or deleted — a simple HIDS (Host Intrusion Detection) baseline
